#include<stdio.h>
#include<conio.h>
main()
{
	 int n,i,count=0;
	 printf("enter a number:");
	 scanf("%d",&n);
	 for(i=2;i<n-1;i++)
	 {
	 	if(n%i==0)
	 	count++;
	 }
	 if(count==0)
	 printf("\n%d is a prime number",n);
	 else
	 printf("\n%d is not a prime number",n);
	 getch();
}
